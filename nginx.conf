worker_processes 1;

error_log /etc/nginx/error.log;

events {
    worker_connections 1024;
}

http {

    server {

        listen 80;
        listen [::]:80;

        server_name _;

        location / {
            return 200 'server is running!';
        }
    }

}